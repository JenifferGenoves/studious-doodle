// CONSTANTES

// let interestRate = 0.3;
// interestRate = 1;
// console.log(interestRate);

// Cuando no queremos que cambie el valor de una variable, usamos una constante
// El valor de una variable puede cambiar, LAS CONSTANTES NO SE PUEDEN REASIGNAR

// const interestRate = 0.3;
// interestRate = 1;
// console.log(interestRate);

//Si no se necesita reasignar la variable usamos Const, si la queremos reasignar usamos let.

//PRIMITIVE TIPES

let name ='Mosh'; 
let age = 30; 
let isApproved = false; 
let firstName =undefined;
let selectedColor = null; // Object

// Para verificar el tipo de variable
typeof name; // String

name = 1; // Number


// variable firstaname, su valor como su tipo no están definidos
// Undefined, es un tipo pero también un valor
// En el ejemplo establecimos undefined como valor, su tipo también está indefinido